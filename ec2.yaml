Resources:
  MyS3Bucket1:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: group6-source-data-bucket # Name of bucket
      Tags:
        - Key: Description
          Value: "This bucket stores raw data from multiple sources."

  MyS3Bucket2:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: group6-final-result-bucket # Name of bucket
      Tags:
        - Key: Description
          Value: "This bucket stores processed data after ETL operations."
